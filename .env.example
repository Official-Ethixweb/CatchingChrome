# Live river conditions for the fishing calendar's "today" outlook.
# Get a free key at https://openweathermap.org/api (Current Weather Data).
# Copy this file to `.env` and paste your key below.
OPENWEATHER_API_KEY=

# Live Google reviews for the Client Log carousel.
# Needs "Places API (New)" enabled on a billing-enabled Google Cloud project:
# https://console.cloud.google.com/apis/library/places.googleapis.com
# Without it the Client Log falls back to the curated reviews in
# src/lib/googleReviews.ts.
GOOGLE_PLACES_API_KEY=

# Contact form delivery. REQUIRED for the form to actually reach Ryan —
# without these the form cannot send, and tells the guest to call or email
# instead (it never claims to have sent).
# 1. Create a key at https://resend.com/api-keys
# 2. CONTACT_TO_EMAIL is the inbox enquiries land in.
# 3. CONTACT_FROM_EMAIL must be on a domain verified in Resend. If unset it
#    uses Resend's onboarding@resend.dev sender, which only delivers to the
#    account owner's own address — fine for a smoke test, not for production.
RESEND_API_KEY=
CONTACT_TO_EMAIL=ryanbfishin@gmail.com
CONTACT_FROM_EMAIL=
